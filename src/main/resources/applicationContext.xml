<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit.xsd
">

    <context:property-placeholder location="classpath:rabbit.properties"></context:property-placeholder>

    <rabbit:connection-factory id="factory"
                               host="${rabbit.Host}"
                               port="${rabbit.Port}"
                               virtual-host="${rabbit.VirtualHost}"
                               username="${rabbit.username}"
                               password="${rabbit.password}"/>

    <rabbit:admin connection-factory="factory"></rabbit:admin>

    <rabbit:queue id="fanout_queue_1" name="fanout_queue_1" auto-declare="true"></rabbit:queue>
    <rabbit:queue id="fanout_queue_2" name="fanout_queue_2" auto-declare="true"></rabbit:queue>

    <rabbit:fanout-exchange id="fanout_exchange" name="fanout_exchange" auto-declare="true">
        <rabbit:bindings>
            <rabbit:binding queue="fanout_queue_1"></rabbit:binding>
            <rabbit:binding queue="fanout_queue_2"></rabbit:binding>
        </rabbit:bindings>
    </rabbit:fanout-exchange>

    <rabbit:template id="rabbitTemplate" connection-factory="factory"></rabbit:template>

</beans>